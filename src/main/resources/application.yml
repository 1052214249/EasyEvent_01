spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/graphql?characterEncoding=utf-8&useSSL=false
    driver-class-name: com.mysql.jdbc.Driver
    type: com.zaxxer.hikari.HikariDataSource #数据库连接池
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl #表示会在日志输出sql语句
    lazy-loading-enabled: true